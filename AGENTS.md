# 装点猫管理后台项目规则

## 项目与产品边界

- 只修改当前项目；管理后台原型基座仅可只读参考。保护已有和未提交改动。
- 业务页面复用 `@/components/foundation`，不得另建页面间距、表格、分页或弹窗规范；表单弹窗优先 `AdminDialog`，旧页面仅在需求涉及处迁移。
- TDesign 官方组件及原型基座完整保留官方视觉。未经明确要求，不新增 CSS、内联样式、深度选择器、全局覆盖或主题变量来改写间距、尺寸、圆角、阴影、颜色、字体与动效；仅官方组件和基座无法表达的业务布局允许必要样式。
- 产品行为以当前实现及有效产品文档为准，不从历史方案反推。项目 UI、权限和 Notion 细节由相关 `.agents/skills` 按需加载。

## 验证与工具

- 后端启动/验证使用 `npm run backend:*`，默认 Docker 提供 JDK/Maven；不因本机无 mvn 跳过验证。
- 按完整调用链选择授权：`task:start`（兼容 `task:micro`）可能访问远程 Git、创建 Worktree、切换预览；`dev:task` 涉及服务与 Docker；`dev:task:check -- --worktree` 核验代理且 full 模式查询 Docker 端口；后端检查和 `verify*` 按计划调用 Docker/监听服务。`test:scripts` 包含回环监听，首次实际执行即用有效最小授权通道，不先制造失败。
- `check:changed --list`、`verify:delivery -- --list`、`verify:local -- --list` 只列计划，可先确认依赖；定向脚本测试按实际能力判断，不把全套需求套到纯函数测试。

| 改动/阶段                             | 本地验证                                                               |
| ------------------------------------- | ---------------------------------------------------------------------- |
| 纯文案、规则、别名                    | 差异和相关格式；语义明确不检索历史或 Notion                            |
| 低风险样式、小交互                    | `check:changed -- <文件>`，必要时补指定交互                            |
| 页面、表单、路由、服务逻辑            | 变更检查加受影响单测/E2E                                               |
| 登录、权限、API、后端、迁移、状态     | 相关前后端及必要真实环境；边界不明用 `check:full`                      |
| 普通正式交付                          | `verify:delivery` 按完整任务差异检查，可靠 CI 负责最终候选全部适用门禁 |
| 高风险完整回归、验证器排障、CI 不可靠 | `verify:local`，强制重验加 `--force`                                   |

- 分类器只有证明无影响才省略无关门禁；未知、共享契约、权限、迁移、依赖及验证器保守升级。保持必需汇总检查，不用整个工作流路径跳过，不以本地通过覆盖 CI 失败。
- 适用测试以实际用例报告证明覆盖、零跳过及零重试，不能只看命令退出码；CI 工作流重跑也不能把首次失败抹去，修复原因后对新候选重新验证。
- 复用仅由执行器证明有效的成功证据；相关输入/依赖/环境/产物变化重验。CI 不用本地缓存，适用测试仍首次通过，不以跳过或重试绿灯放行。规则细节见 `efficient-code-delivery/references/verification-evidence.md`。
- `.task-verification/` 保存证据与耗时，`.task-runtime/` 保存当前 Worktree 运行身份/缓存，均不入 Git。区分新执行、有效复用、不适用和未运行。
- Mock 页面/隔离后端测试不隐式启动共享真实后端；必要预览身份、数据库保护与正式交接仍现场证明。

## 批次与预览

- 工程入口见 `.codex/zdm-project-workflow.yaml`。启动/跨对话恢复先 `task:batch -- status --json`，复用有效登记；无批次才 `task:start -- --slug <batch-slug> --kind business` 从最新 origin/main 新建。基建用 `--kind infrastructure`，不占活跃业务目录。旧任务只在明确恢复并核对范围后登记。
- 同批次共用分支、Worktree 和 5175；不同顶层对话串行交接。模块变化不重建环境；归属不明、其他任务占用或登记失效时保留现场。固定项目目录作为 main 基线锚点。
- 恢复旧 Worktree 时区分已发布协作流程与该目录实际脚本能力；不恢复旧的逐反馈推送默认，不复制主线覆盖业务改动。新命令缺失时执行已有充分验证及已证明兼容的运行入口，不能把候选脚本当作已发布能力。
- macOS 仅首次 `dev:task:install` 安装常驻服务，以后 `dev:task` 恢复/切换任务。先核对真实状态，健康则复用；状态查询不应隐式启动旧环境。
- 用户可见前端改动首次结果前以 `dev:task:check` 证明 5175 的 Worktree、分支、模式、API 和代理健康。无需浏览器验收不豁免预览；不能只看端口存活。
- 纯前端使用任务前端+集成后端；生产后端、API、迁移、运行配置变化使用任务前端+任务后端；共同使用唯一集成 MySQL。测试/工具变化与生产运行变化分别判断，不把所需任务后端降级。
- 固定集成 Worktree 使用 5173，只运行正式汇入的组合；仅明确并行对比才用 `dev:task -- --temporary`。
- Flyway/显式数据库高风险走启动器保护，冲突/失败保留锁与备份，不自动恢复。数据、卷、备份、容器及日志清理与 Git 交付分开授权。
- `dev:task:stop` 停止当前任务前端和后端，5175 回退5173；交接不能留下失效页面/API或阻塞下个任务的锁。

## Git 与交付适配

- main 稳定主线，任务默认 `codex/`；main 与 codex/integration-current 不直接提交普通改动。批次授权、模块本地 checkpoint、完整收口和安全清理按用户级规则及 git-task-lifecycle，不按每条反馈自动推送。未登记旧任务沿用原授权。
- 当前 GitHub 插件 PR 写入403，写 PR 直接使用已认证 gh CLI；普通 Git 用本地Git和origin，代码推送用SSH。
- main 的远端保护要求 `Frontend quality`、`Backend quality` 与最新主线候选通过，管理员同样受约束，禁止强推和删除；交付时核对实际保护状态，不能仅凭文档认定已启用。
- 批次交付前 `task:batch -- plan --delivery` 核对模块/提交；暂停或排除但仍在候选的代码先处理并验证。回滚计划核对传递依赖和真实消费者；改变关联产品行为或恢复数据库须另有准确授权。
- 获交付授权并推送后及时启动当前候选 PR/CI，再按适配的 `delivery.sync_integration` 汇入固定集成；默认使用已发布兼容启动器。启动器自身升级使用适配的 `reviewed_launcher_candidate` 和已审查准确 SHA，不能先运行旧集成实现；只读计划不证明远端实时状态。同步必须停止旧任务、恢复并验证集成、释放所属数据库锁并保留备份；任何必要交接失败均不得报告完成。
- 集成用于组合验收，main 按准确任务分支/清单合并，不合入滚动集成整体。正式验收核对最新 origin/main、实际候选和有效证据；候选变化补验，不机械重复全套。合并不等于生产发布。
- 完整收口证明本地 main 与远程 SHA 一致、运行交接完成，再安全清理已授权的当前批次代码载体，`task:batch -- close` 归档索引。该命令不能代替CI、实时远程/运行证据或安全清理；保留 main、固定集成、其他任务及数据资产。
