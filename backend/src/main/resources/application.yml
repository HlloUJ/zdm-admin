server:
  port: 8080

spring:
  application:
    name: zdm-platform-backend
  datasource:
    url: ${ZDM_DB_URL:jdbc:mysql://127.0.0.1:3306/zdm_admin?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${ZDM_DB_USERNAME:zdm_admin}
    password: ${ZDM_DB_PASSWORD:zdm_admin_pwd}
  flyway:
    enabled: true
    locations: classpath:db/migration

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html

zdm:
  security:
    dev-tokens-enabled: ${ZDM_SECURITY_DEV_TOKENS_ENABLED:false}
    verification-code: ${ZDM_SECURITY_VERIFICATION_CODE:}
    session-duration: ${ZDM_SECURITY_SESSION_DURATION:PT12H}
