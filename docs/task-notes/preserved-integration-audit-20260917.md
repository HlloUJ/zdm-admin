# 旧集成差异审查（2026-09-17）

## 范围与依据

用户批准逐项审查旧集成差异：只将必要且符合现有约定的内容补入 main，重复、过时或未经批准的行为变更不恢复，核对后清理保留分支。

- 审查基线：main `e1533a4`。
- 保留快照：`codex/integration-preserved-before-employee-handoff-20260917`，提交 `ef11befca0357682445c8feb1cb4091bcd0cce2c`。
- 集成对齐提交：`155d27e9fc5f4a1e0bbc8c1e83816690f41f3b6b`；其文件树与审查基线一致，保留快照是其祖先。
- 本次只补测试，不改变业务实现、名称、页面、接口和数据库。历史代码的存在不等于产品变更已获批准。

## 九个差异文件的处理

| 文件                                  | 审查结果与处理                                                                                                                                                                                                                                                                          |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ProductAttribute.java                 | 仅空行差异，不迁入。                                                                                                                                                                                                                                                                    |
| ProductAttributeController.java       | 可见 scope 的同等循环由独立方法改为内联，无功能补缺，不迁入。                                                                                                                                                                                                                           |
| ProductAttributeService.java          | 主线已有数据库唯一索引与重复异常处理；旧代码另外 trim 名称、提前查重并改提示文案，会改变接口约定。未找到此次要求恢复这些行为的依据，不迁入。                                                                                                                                            |
| ProductAttributeValueController.java  | 主线已经按 scope、select 类型、未删除状态查询候选属性；旧实现额外计算模板引用数，无当前消费需求，不迁入。                                                                                                                                                                               |
| src/pages/product/attribute/index.vue | 排版与变量移动不迁入；保留主线 openCreate 权限判断；不恢复旧响应式 CSS 或新增文案/布局变化。                                                                                                                                                                                            |
| PlatformApiSmokeTest.java             | 迁入属性和属性值两项后端权限回归，验证 self/all、Tab 权限、禁止未授权操作及创建人。旧 CRUD 中创建人、持久化、删除已有 ProductAttributeLifecycleApiTest 等覆盖；额外的 trim 和旧文案预期不迁入。种子与索引断言属于旧迁移保障，迁移本身在主线且本次不变；不恢复其他供应商测试的 ID 替换。 |
| tests/e2e/admin-api-mocks.ts          | 不整体替换公共夹具，避免牵动其他测试；新增 Tab 测试可使用现有数据。                                                                                                                                                                                                                     |
| tests/e2e/feedback-foundation.spec.ts | 路由和定位变化为旧夹具配套调整，现有夹具未替换，无需迁入。                                                                                                                                                                                                                              |
| tests/e2e/permission-center.spec.ts   | 以新增方式迁入两项多 Tab 授权与首个可访问 Tab 回退测试，保留主线原有测试；不恢复用全局数据假设替换现有测试的做法。模板断言移动与等价写法不迁入。                                                                                                                                        |

## 清理边界

验证、主线交付与集成交接完成后，删除旧保留分支和本次审查分支及 Worktree。旧快照仍可从集成分支历史按上述 SHA 追溯；删除的是冗余分支引用，不改写历史。数据库备份、验证记录、其他任务分支及 Worktree 不在清理范围。

## 验证结果

`npm run verify:local` 通过：83 项前端单测、54 项脚本测试、148 项 Chrome E2E、190 项后端测试（无失败、无跳过），并通过类型、Lint、格式、构建、Checkstyle 和 SpotBugs 检查。原业务源码与 main 无差异。日志保存在 `/tmp/preserved-integration-audit-verify.log`。测试库关闭期间出现定时任务连接日志，测试与质量门禁最终成功，不属于本次新增业务行为。
